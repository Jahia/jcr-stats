query getTree($path: String, $maxDepth: Int) {
    jcrStats {
        tree(path: $path, maxDepth: $maxDepth) {
            name
            size
            nodeCount
            children {
                name
                size
                nodeCount
                children {
                    name
                    size
                    nodeCount
                }
            }
        }
    }
}
